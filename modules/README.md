# modules

Business module ownership shells.

Phase 1 modules are importable placeholders only. They must not import
`apps`, `adapters`, `workflows`, provider SDKs, network clients, database
clients, environment-variable readers, or real business data.
