# migrations

Migration namespace placeholder.

P01-01 does not create database schemas, ORM mappings, connection strings, or
runtime migration commands. Future migrations must be reversible, scoped, and
free of real data.
