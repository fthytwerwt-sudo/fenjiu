# Codex 执行包（已被 Phase 0–8 任务卡取代）

> **SUPERSEDED：** 本文件保留为未合并早期规划的兼容入口，不能再直接下发。请使用 [CODEX_EXECUTION_INDEX.md](CODEX_EXECUTION_INDEX.md) 和 `codex_tasks/phase_00` 至 `phase_08` 的单张任务卡。

早期执行包使用的 Phase 编号与本轮 `Phase 0–8` 纵向实施图不一致，也没有将真实供应链资料入场、fixture 切换、全链回归和 `run-ready` 拆成可独立验收的卡。因此它不再是权威执行入口。业务范围、外部动作默认关闭、Git 完成和状态边界仍遵守仓库 `AGENTS.md`。
