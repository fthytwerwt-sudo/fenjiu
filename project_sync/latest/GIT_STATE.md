# Git 状态｜GIT_STATE

- 生成时分支：`main`
- 生成时来源 commit：`ae84e183be38da62d17c8567569f75206ddb35f1`
- 远端：`unavailable`
- 生成时工作区：`dirty`

说明：`source_commit` 是生成同步包时工作区所基于的 commit，不等于之后提交同步包目录的 commit，因此不会形成 Manifest 自我引用。新会话开始写入前必须重新运行 `git status --short --branch`。
